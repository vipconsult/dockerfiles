common:
    restart: always
    volumes:
        - /etc/localtime:/etc/localtime:ro
        - /etc/timezone:/etc/timezone:ro
        - ~/.bashrc:/root/.bashrc
        - ~/.bash_history:/root/.bash_history
    env_file:
        - env 
